const std = @import("std");
const ast = @import("../ast/ast.zig");

// Ported from submodule/typescript-go/internal/transformers/moduletransforms/

pub fn transformModule(context: anytype) void {
    _ = context;
}
