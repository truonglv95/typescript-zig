const std = @import("std");
const ast = @import("../ast/ast.zig");

// Ported from submodule/typescript-go/internal/transformers/estransforms/

pub fn transformES(context: anytype) void {
    _ = context;
}
