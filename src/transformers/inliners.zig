const std = @import("std");
const ast = @import("../ast/ast.zig");

// Ported from submodule/typescript-go/internal/transformers/inliners/

pub fn transformInliners(context: anytype) void {
    _ = context;
}
