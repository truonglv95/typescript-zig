const std = @import("std");
const ast = @import("../ast/ast.zig");

// Ported from submodule/typescript-go/internal/transformers/jsxtransforms/

pub fn transformJsx(context: anytype) void {
    _ = context;
}
