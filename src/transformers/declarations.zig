const std = @import("std");
const ast = @import("../ast/ast.zig");

// Ported from submodule/typescript-go/internal/transformers/declarations/

pub fn transformDeclarations(context: anytype) void {
    _ = context;
}
